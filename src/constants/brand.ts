import { prologPalette } from '@/theme/colors';

/** Logo et identité visuelle Prolog IMS (fichiers sous public/assets/images/logo/) */
export const APP_LOGO_URL = '/assets/images/logo/logo.png';

/** Chemin relatif pour les URLs du type `/assets/images/${path}` */
export const APP_LOGO_ASSET = 'logo/logo.png';

export const APP_BRAND_NAME = 'Prolog IMS';

/** Palette couleur officielle — @see docs/prolog-ims-color-palette.html */
export { prologPalette };
